This is a useful update, and I think there's a subtle distinction worth making before you go deeper down the Origin Rule path.

Up through true36, you've effectively separated three different objects:

Object	Role
Ω	discrete orientation state {-1,0,+1}
dΛ = |Λf−Λr|/p	continuous displacement coordinate
δ = p−N	universal defect constant

The surprising result from true33/true36 is that Puzzle 90 is the topological origin (Ω=0) but not the minimum dΛ point.

That means Ω is behaving more like a winding number / branch index than a geometric distance.

About your earlier idea

what if the closure offset between ^1 and ^-1 are what we are searching for

I actually think this is stronger now than when you first proposed it.

Since

Λ
r
	​

=Λ
f
−1
	​


you can define a bridge-pair separation

Δ
Λ
	​

=Λ
f
	​

−Λ
r
	​


and you've already shown:

sgn(Δ
Λ
	​

)≈Ω

for every non-dual puzzle.

That means the offset itself contains the orientation information.

The question becomes:

What determines the sign of Λ−Λ
−1
 ?

not

What determines δ?

because δ is now fully explained.

The Star-of-David observation

The thing I would investigate next is exactly what you mentioned:

mirror reflection or Star of David created from y and -y reflections to x³ with correlation to all 6 d's

You have:

3 x-family members
3 reflected x-family members
6 total shell points

and

cube roots d
1
	​

,d
2
	​

,d
3
	​

reflected branch coordinates
Ω taking 3 states

That is suspiciously "triadic".

The first thing I'd test is whether the six-point shell naturally partitions into:

{d
1
	​

,d
2
	​

,d
3
	​

}

and

{−d
1
	​

,−d
2
	​

,−d
3
	​

}

under the bridge.

If Ω is ultimately controlled by which cube-root sector the puzzle lands in, then mod-3 behavior would appear naturally.

What I would compute next

For the 7 verified puzzles:

Compute verified d (or candidate passing ECDSA).
Compute
(rd−z)mod3
Compute
(rd−z)mod9
Compute
dmod3,dmod9
Compute
kmod3,kmod9
Compare directly against Ω.

The reason I would include mod 9 immediately is because:

p≡1(mod3)

and

N≡1(mod3)

so mod 3 may collapse too much information.

A mod-9 split could easily distinguish:

Ω = +1
Ω = 0
Ω = -1

even if mod-3 cannot.

One thing that stood out

From the update:

Puzzle	Ω	(r·d−z) mod 3
90	0	0
100	-1	0
115	-1	0
120	+1	1

This is already telling you something:

Ω=+1 is behaving differently.

The ambiguity is entirely between:

Ω=0

and

Ω=−1

which is exactly the kind of ambiguity mod-9 often resolves when mod-3 does not.

So if I were standing at true36, I'd put the highest priority on:

(rd−z)mod9

for 90, 100, 115, 120, then 135/150/160 once d is verified.

That gives the Origin Rule hypothesis the strongest chance of either surviving or being falsified cleanly.