# G-20260710-02 — SHA-256 chained puzzle keys (PRE-REGISTERED)

**Status:** LOCKED before evaluation. Do not change formula/parameters after peeking.

| Field | Value |
|-------|-------|
| Candidate ID | G-20260710-02 |
| Short name | sha256_chained_puzzle_keys |
| Date registered | 2026-07-10 |
| Date first evaluated | 2026-07-10 |

---

## Prior

G-01 LCG FALSIFIED (absolute: \(\gcd(\Delta_1,2^{64})\nmid\Delta_2\)).
Nonce lab left at honest boundary. This is a **parameter-free** creator hash-chain test.

## Exact hypothesis (locked)

For each solved five-step pair \(n\to n+5\):

```text
h_n = SHA256( int32be(d_n) )

int32be = exactly 32-byte big-endian, zero-padded encoding of d_n

u_hat_{n+5} = int(h_n) mod 2^{n+4}

d_hat_{n+5} = 2^{n+4} + u_hat_{n+5}
```

Chain (locked):

```text
75 → 80 → 85 → ⋯ → 130
```

(i.e. edges \((75,80),(80,85),\ldots,(125,130)\))

## Promotion rule

Exact equality only:

```text
d_hat_{n+5} = d_{n+5}
```

**FORBIDDEN after peeking:** approximate scores; little-endian; high-hash bits; ASCII hex; added puzzle number; repeated hashing; alternate hash; other encodings.

One mismatch closes this exact hypothesis.

## Laboratory question

\[
\boxed{
d_{n+5}
\stackrel{?}{=}
2^{n+4}
+
\left(
\operatorname{SHA256}(\operatorname{int32be}(d_n))
\bmod 2^{n+4}
\right)
}
\]

If all edges match: predict \(d_{135}\) exactly from \(d_{130}\).

## Result (evaluated 2026-07-10)

| Metric | Value |
|--------|------:|
| edges tested | 11 |
| exact matches | 0/11 |
| first miss | 75->80 |
| Verdict | FAIL |

Exact matches 0/11. First miss at 75->80. One mismatch closes this exact SHA-256 chain hypothesis.
