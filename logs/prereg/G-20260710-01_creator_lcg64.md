# G-20260710-01 — Creator-side 64-bit LCG on five-step chain (PRE-REGISTERED)

**Status:** LOCKED before evaluation. Do not change formula/parameters after peeking.

| Field | Value |
|-------|-------|
| Candidate ID | G-20260710-01 |
| Short name | creator_payload_lcg_64 |
| Date registered | 2026-07-10 |
| Date first evaluated | 2026-07-10 |

---

## Boundary

Nonce lab closed at honest limit (RFC6979 batch attribution; no algebraic keyspace cut).
This branch tests **creator-side payload generation**, not spender nonces.

## Exact hypothesis (locked)

For solved puzzles:

```text
u_n = d_n - 2^{n-1}

w_n = floor( 2^{64} * u_n / 2^{n-1} )     # locked 64-bit band location
```

Chain (locked — regular five-step only):

```text
n = 75, 80, 85, ..., 130
```

LCG (locked):

```text
w_{n+5} ≡ a * w_n + c  (mod 2^{64})
```

**Infer** `(a,c)` from the first three values `(w_75, w_80, w_85)` only:

```text
w_80 ≡ a * w_75 + c
w_85 ≡ a * w_80 + c
⇒ a ≡ (w_85 - w_80) * (w_80 - w_75)^{-1}  (mod 2^{64})
  c ≡ w_80 - a * w_75  (mod 2^{64})
```

If `(w_80 - w_75)` is not invertible mod `2^{64}`, the hypothesis fails immediately (no alternate modulus).

**Freeze** `(a,c)`. Predict `w_90, w_95, ..., w_130` exactly.

**FORBIDDEN after peeking:** width sweep; alternate modulus; byte reversal; approximate match; other step sizes; using n outside the locked chain for inference.

## Promotion gate

\[
\boxed{\text{Every locked holdout prediction must match exactly.}}
\]

Holdout: predictions for `n ∈ {90,95,...,130}` (9 values). One miss ⇒ **64-bit normalized LCG branch CLOSED**.

If all holdout matches succeed: predict `w_135` (top 64 bits of the 134-bit payload) ⇒ remaining search `2^{70}` — genuine keyspace reduction.

## Laboratory question

\[
\boxed{\text{Do the normalized puzzle payloads follow a fixed creator-side recurrence?}}
\]

## Result (evaluated 2026-07-10)

| Metric | Value |
|--------|------:|
| a, c | n/a, n/a |
| holdout exact matches | 0/9 |
| Verdict | FAIL |
| w_135 (if promoted) | None |

(w_80 - w_75) = 11727712612230228442 is even => not invertible mod 2^64. Locked LCG hypothesis fails immediately (no alternate modulus).
