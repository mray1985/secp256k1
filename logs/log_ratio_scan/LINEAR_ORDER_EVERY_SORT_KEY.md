# Linear rank charts — every sort key

Combined: [`LINEAR_ORDER_ALL_IN_ONE_EVERY_SORT_KEY.txt`](LINEAR_ORDER_ALL_IN_ONE_EVERY_SORT_KEY.txt)

Per-key folder: [`linear_rank_charts_by_each/`](linear_rank_charts_by_each/)

Each series gets one turn as the **row order** (linear column); all other ranks sit to the right.
