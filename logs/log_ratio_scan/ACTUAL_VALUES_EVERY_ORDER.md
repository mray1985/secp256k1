# Actual values — every sort order

Folder: [`actual_values_every_order/`](actual_values_every_order/)

- Master table: [`actual_values_every_order/VALUES_MASTER_BY_n.csv`](actual_values_every_order/VALUES_MASTER_BY_n.csv)
- Per sort key: `VALUES_BY_<key>.csv` (wide) and `.txt` (per-puzzle blocks)
