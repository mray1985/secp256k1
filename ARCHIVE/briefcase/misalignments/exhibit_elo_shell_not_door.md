# EXHIBIT: e_lo shell, not door

## Three-slice hinge scan

```text
implemented correctly

e_lo = (n−1)/256:
  nearest slice in 79/82 puzzles

full-set rho ≈ +0.70:
  confounded by puzzle index / bit length

band 65–130 rho ≈ +0.017:
  no real scalar compass
```

## Meaningful discovery

```text
The lower range slice e_lo is the most stable normalization shell.
```

```text
Use e_lo for:
  candidate comparison
  solved-puzzle normalization
  distance-to-public-fingerprint scoring

Do not use e_lo for:
  deriving d directly
```

## Verdict

```text
REJECTED:
  three-slice power as scalar predictor

VALID:
  e_lo as preferred normalization shell

NEXT:
  local-band residual scan
  high-bit public-control scan
  candidate gate integration
```

```text
e_lo wins the shell; it does not open the door.
```

Judge Popcorn: **we found the best lens, not the star’s address.**
