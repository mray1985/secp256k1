# Factoradic native-lead — FALSIFIED archive

**Status:** FALSIFIED as a pub/private pairing invariant.  
**Use:** filter / null model for factorial sawtooth scale-drift — **not** a solver.

## Compact claim line

> Native \(m=n\) factoradic lead fractions give \(r=0.61\), but random and shuffled nulls produce similar correlations (\(p\approx 0.14\)–\(0.24\)); therefore the effect is explained by common bit-width/factoradic drift rather than paired public/private structure.

## Do not rediscover

If a future probe reports “\(d\) factoradic lead tracks \(Px\) lead,” check this folder first. The apparent \(r\approx 0.61\) is **background rhythm** from shared bit-width \(n\), not \(d\mapsto [d]G_x\).

**Stop optimizing** lead width, offsets, or thresholds on this surface — that mines the same artifact.

## What remains useful

| Keep | Role |
|------|------|
| Native \(m=n\) transform | **Control feature** — any new invariant must beat it under shuffle/random nulls |
| Sawtooth / ladder on \(d\) | Representation fact only |
| Null suite A–E | Template for “does this need the real pairing?” |

## Three products of this failed idea

1. **Frozen false positive** — this folder.
2. **False-positive benchmark** — \(0.610-0.485=0.125\) with \(p\approx 0.15\). Ordinary coincidence shape, not a magical cutoff.
3. **Promotion gate** — the real relation must break when \(d\) is attached to the wrong public point.

```text
"Looks similar"  ≠  "depends on the correct pairing."
Ask:  P_i = [d_i]G  versus  P_π(i) ≠ [d_i]G
```

The sawtooth itself is **not** the key signal. Factoradic patterns appear in random same-width numbers; \(d\) and \(P_x\) looking similar is not enough.

Promote **only if** all hold:

```text
advantage > 0.12
p_shuffle < 0.01
beats random n-bit
beats random EC pairs
holds out-of-sample
direction consistent across puzzle ranges
```

**Pre-register** before peeking: `prereg/F_PREREG_TEMPLATE.md`  
**Exclude:** \(F=\mathbf{1}\{P=[d]G\}\) and curve-membership-only formulas.

Harness: `../../pairing_advantage_filter.py` · `PAIRING_PROMOTION_GATE.md`

## Contents

```
logs/     evidence digest, lead sweeps, null distributions
scripts/  reproduction scripts (frozen copies)
README.md this file
```

Primary digest: `logs/FACTORADIC_EVIDENCE_DIGEST.md`
